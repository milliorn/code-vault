# import classes
from graph import Graph, build_graph
from vertex import Vertex

# Make an excavation_site variable inside script.py and
# assign it to the build_graph() function we’ve supplied.
excavation_site = build_graph()

# call the .explore() method on excavation_site.
excavation_site.explore()